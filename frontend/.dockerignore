# Huge, and platform-specific. The image installs its own via `npm ci`.
node_modules

# Build output is produced inside the image by `ng build`.
dist

# Angular's local build cache — large and useless in an image.
.angular

# Secrets must never enter a layer.
.env
.env.*

# Not needed at build time.
.git
coverage
*.log
.vscode
